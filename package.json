{
  "name": "pankaj-medical",
  "version": "1.0.0",
  "private": true,
  "description": "Monorepo for Pankaj Medical and General Stores Pharmacy E-Commerce Web Application",
  "workspaces": [
    "client",
    "server"
  ],
  "scripts": {
    "bootstrap": "npm install && npm install --prefix client && npm install --prefix server",
    "dev:client": "npm run dev --prefix client",
    "dev:server": "npm run dev --prefix server",
    "dev": "concurrently --kill-others-on-fail \"npm run dev:server\" \"npm run dev:client\"",
    "lint:client": "npm run lint --prefix client",
    "lint:server": "npm run lint --prefix server",
    "lint": "npm run lint:client && npm run lint:server",
    "format:client": "npm run format --prefix client",
    "format:server": "npm run format --prefix server",
    "format": "npm run format:client && npm run format:server"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
